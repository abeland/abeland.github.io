# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
author: Abe Land
title: Hello. I'm Abe.
email: abe@lostfoundlabs.com
description: > # this means to ignore newlines until "baseurl:"
  I wrote code for Facebook for a long time and now I write code for myself.
  I created this site mostly so I could write things down which I would otherwise forget.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username: abeland

# Build settings
markdown: kramdown
theme: jekyll-whiteglass

gems:
  - jekyll-archives
  - jekyll-paginate
  - jekyll-sitemap

paginate_path: /posts/:num/
paginate: 5

plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
